{
  "name": "mediurg",
  "version": "1.0.0",
  "description": "Application PWA hors ligne de pharmacologie d'urgence pour SAUV, SMUR et SAU",
  "private": true,
  "type": "module",
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "node scripts/check-versions.cjs && vite build && node scripts/generate-asset-manifest.cjs",
    "preview": "vite preview",
    "check-versions": "node scripts/check-versions.cjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^25.0.1",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
