node_modules/
build/
coverage/
.env
.env.local
.env.*.local
npm-debug.log*

.vercel

# Claude Code config (peut contenir des clés API / MCP secrets)
.claude/
.mcp.json
.env*.local

# Codex local config (peut contenir des cles API / MCP secrets)
.codex/
