#!/usr/bin/env sh
# Lance la suite de tests avant chaque `git push`. CI rattrape ensuite mais
# c'est plus rapide d'apprendre une régression localement avant que tout le
# pipeline GitHub Actions tourne (lint + typecheck + format + knip + test +
# build).
#
# Pour bypass exceptionnel : `git push --no-verify`. À éviter — la mémoire
# acr_ux signale que MediURG est utilisé en stress réel, on ne veut pas
# pousser une régression dans le mode ACR.
npm test
