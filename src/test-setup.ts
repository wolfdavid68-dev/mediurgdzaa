// Auto-importé par vitest avant chaque suite (cf. vite.config.ts → test.setupFiles).
// Étend expect avec les matchers jest-dom : toBeInTheDocument, toHaveClass,
// toHaveAttribute, etc. — bien plus lisibles que querySelector + assertions manuelles.
import "@testing-library/jest-dom/vitest";
